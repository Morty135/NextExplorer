<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Worker Shares</title>
  <style>
    body { font-family: Arial; margin: 30px; }
    table { width: 100%; border-collapse: collapse; margin-top: 20px; }
    th, td { padding: 10px; border: 1px solid #ccc; text-align: left; }
    .accepted { color: green; font-weight: bold; }
    .rejected { color: red; font-weight: bold; }
  </style>
</head>
<body>

  <h1>Worker: <%= worker.name || worker._id %></h1>
  <p>Miner: <strong><%= worker.miner.username %></strong></p>

  <a href="/miner/<%= worker.miner._id %>">← Back to Miner</a>

  <h2>Shares</h2>

  <table>
    <thead>
      <tr>
        <th>Timestamp</th>
        <th>Difficulty</th>
        <th>Status</th>
        <th>Block Height</th>
      </tr>
    </thead>
    <tbody>
      <% shares.forEach(s => { %>
        <tr>
          <td><%= s.timestamp.toLocaleString() %></td>
          <td><%= s.difficulty %></td>
          <td class="<%= s.accepted ? 'accepted' : 'rejected' %>">
            <%= s.accepted ? "Accepted" : "Rejected" %>
          </td>
          <td><%= s.height %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>

</body>
</html>
